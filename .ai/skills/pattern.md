# Skill: Create Pattern

1. Check `docs/patterns/` for an existing pattern.
2. Compose primitives; put reusable pieces under `src/components/patterns` in the library only if cross-app.
3. Document When / Composition / Do / Don't.
