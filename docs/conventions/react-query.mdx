import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Conventions/React Query" />

# React Query

- Query keys: `[resource, params]`.
- Mutations invalidate related queries.
- Use `isPending` for button disable + spinner.
