import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Conventions/Performance" />

# Performance

- Import only what you use from `@erp/miniapp-ui` (ESM tree-shake).
- Virtualize huge tables (long-term Data Grid).
- Avoid layout thrash in drawers/dialogs.
