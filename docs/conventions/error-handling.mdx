import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Conventions/Error Handling" />

# Error handling

Load → `ErrorState`. Mutation → toast + field errors. Permission → `PermissionState`.
