import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Conventions/Naming" />

# Naming

- Components: PascalCase files only if local feature components; library stays kebab-case files.
- Hooks: `use-thing.ts` → `useThing`.
- API routes: kebab-case.
