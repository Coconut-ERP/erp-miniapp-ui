import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Conventions/Hooks" />

# Hooks

- Data hooks wrap React Query (or app equivalent).
- Keep UI components free of ERP client imports when possible — pass data via props.
