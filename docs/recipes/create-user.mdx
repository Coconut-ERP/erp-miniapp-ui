import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Recipes/Create User" />

# Recipe: Create user

## Goal
Admin creates a user record.

## Steps
`PageHeader` "Create user" → `FormStack` → Submit `Button` → redirect to detail.
