# Recipe: Profile

## Goal
View/edit the current user profile.

## Steps
1. Load profile → `LoadingRows` then form.
2. `PageHeader` + `FormStack` fields.
3. Save → toast success; `ErrorState` on load failure.
