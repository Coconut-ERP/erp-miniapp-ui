# Recipe: Approve request

## Goal
Approve a pending request with optional note.

## Steps
1. Detail view with status `Badge`.
2. Primary `Button` Approve opens `Dialog` with optional `Textarea`.
3. Confirm → mutation → toast → navigate back/list.
